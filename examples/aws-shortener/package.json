{
  "name": "qr-forge-aws-shortener",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal URL shortener for QR Forge (API Gateway + Lambda + DynamoDB)",
  "main": "handler.js",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.848.0",
    "@aws-sdk/lib-dynamodb": "^3.848.0"
  }
}
